<!--singular resource-->
<%= form_for @group, url:{:action=>'create'}, :html => { :class => 'form-horizontal'} do |f| %>
<%#<%= form_for @group, :html => { :class => 'form-horizontal'} do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit nil, :class=>'btn' %>
<% end %>