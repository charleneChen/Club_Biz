<%- model_class = Student -%>

<div class="row">
    <div class="span9">
      <div class="page-header">
        <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
      </div>

      <!--<dl class="dl-horizontal">-->
        <!--<dt><strong><%#<%= model_class.human_attribute_name(:name) %>:</strong></dt>-->
        <!--<dd><%#<%= @student.name %></dd>-->
        <!--<dt><strong><%#<%= model_class.human_attribute_name(:email) %>:</strong></dt>-->
        <!--<dd><%#<%= @student.email %></dd>-->
        <!--<dt><strong><%#<%= model_class.human_attribute_name(:password) %>:</strong></dt>-->
        <!--<dd><%#<%= @student.password %></dd>-->
      <!--</dl>-->

      <!--<div class="form-actions">-->
        <%#<%= link_to t('.back', :default => t("helpers.links.back")),
                      students_path, :class => 'btn'  %>
      <%#<%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_student_path(@student), :class => 'btn' %>
      <%#<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
#                      student_path(@student),
#                      :method => 'delete',
#                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-danger' %>
      <!--</div>-->

      <div>
        <p><strong>My Following Events</strong></p>
        <table class="table table-striped">
          <thead>
              <th>Event_ID</th>
              <th>Event_Title</th>
              <th>Event_Type</th>
              <th>Actions</th>
          </thead>
          <tbody>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
          </tbody>
        </table>
      </div>

      <div>
        <p><strong>My Joined Societies</strong></p>
        <table class="table table-striped">
          <thead>
              <th>Society_ID</th>
              <th>Club_Name</th>
              <!--<th>Joined_At</th>-->
              <!--<th>Actions</th>-->
          </thead>
          <tbody>
            <% @student.societies.each do |society| %>
              <td><%= society.id %></td>
              <td><%= society.club_name %></td>
              <!--<td></td>-->
            <% end %>
          </tbody>
        </table>
      </div>

    </div>
    <div class="span3" style="margin-top: 75px;">
      <div>
        <ul class="nav nav-pills nav-stacked">
          <li class="active">
            <a href="#">Home</a>
          </li>
          <li>
            <a href="/students/<%= @student.id %>/edit">Settings</a>
          </li>
          <li>
            <a href="/students/<%= @student.id %>/societies">Join Clubs</a>
          </li>
          <li>
            <a href="#">Reservations</a>
          </li>
          <li>
            <a href="#">
              <span class="badge pull-right">3</span>
              Messages
            </a>
          </li>
        </ul>
      </div>

      <div class="well">
        <h4>About Me</h4>
        <ul>
          <li>name: <%= @student.name %></li>
          <li>password: <%= @student.password %></li>
          <li>email: <%= @student.email %></li>
        </ul>
      </div>
    </div>
</div>
